<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="/static/css/survey.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.2.0/chart.min.js"></script>
    <script src="/static/js/base.js"></script>
    <script src="/static/js/survey.js"></script>
    <title>#{data.title}</title>
</head>
<body>
    include header
    <div class="root mt-5">
        if isAuthor
            <div class="card survey-card>
                <div class="card-body>
                    <h5 class="card-text"> #{data.title}</h5>
                    <p class="card-text"> #{data.description}</p>
                    <canvas id="histogramContainer"></canvas>
                    if !wasEnd
                        <form name="closeSurvey">
                            <button type="button" class="btn btn-outline-danger" onclick="setClosed('#{data.id}')">Закрыть опрос</button>
                        </form>
                    else
                        <p class="card-text"> Опрос быз закрыт Вами</p>
                </div>
            </div>
            <script src="/static/js/generateHistogram.js" async></script>
        else if hasVoted
            <div class="card survey-card>
                <div class="card-body>
                    <h5 class="card-text"> #{data.title}</h5>
                    <p class="card-text"> #{data.description}</p>
                    <canvas id="histogramContainer"></canvas>
                </div>
            </div>
            <script src="/static/js/generateHistogram.js" async></script>
        else if wasEnd
            <div class="card survey-card>
                <div class="card-body>
                    <h5 class="card-text"> Опрос закрыт автором</h5>
                    <p class="card-text"> Вы не участвовали в этом опросе и не можете увидеть его результаты</p>
                    <canvas id="histogramContainer"></canvas>
                </div>
            </div>
        else
            <div class="card survey-card>
                <div class="card-body>
                    <h5 class="card-text"> #{data.title}</h5>
                    <p class="card-text"> #{data.description}</p>
                    <form name="response">
                        <div>
                            each val, index in data.results
                                <label><input type="#{data.config}" name="sex" value="#{index}">#{index}</label>
                        </div>
                        <button type="button" onclick="">Отправить ответ</button>
                    </form>
                </div>
            </div>
    </div>
</body>
</html>